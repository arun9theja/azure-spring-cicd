package hello;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;

@SpringBootTest  // Ensure that the full application context loads correctly
public class ApplicationTest {

    @Test
    public void contextLoads() {
        // The test passes if the application context loads without errors
    }
}
